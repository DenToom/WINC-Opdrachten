// 2d Write a simple JavaScript program to join all elements of the following array 
// into a string (with spaces). *ps: deze opdracht heb ik engels gelaten omdat er een
// dikke tip in zit over welke array method je gaat gebruiken. 

console.log(stringsTogether(['Winc', 'Academy', 'is', 'leuk', ';-}']))

function stringsTogether(array) {
	return array.join(' ')
}
